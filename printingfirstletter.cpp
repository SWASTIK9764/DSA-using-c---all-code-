#include <iostream>
using namespace std;
// when char data type is used then only one char is print
int main() {
    char name;
    cout<<"Enter your name:";
    cin>>name;
    cout<<name;

    return 0;
}